//import { DataManagerContext } from '../components/context/context';

export function createData(fullname, calories, fat, carbs, protein) {
  return {
    fullname,
    calories,
    fat,
    carbs,
    protein,
  };
}
